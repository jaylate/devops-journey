apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: url-shortener-hc
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        port: 8000
        requestPath: /health
  targetRef:
    group: ""
    kind: Service
    name: {{ .Release.Name }}
